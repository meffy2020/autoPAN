# autoPAN 개발지침

## 1. 목적

이 프로젝트는 노원청소년센터 나놀다판 운영을 위한 현장형 웹 시스템이다.
종이 접수증과 수기 스프레드시트 운영을 대체하며, 다음 3개 화면을 중심으로 개발한다.

- `kiosk`: 학생 접수, 재방문 조회, 신규 등록, 자원 선택, 대기 등록
- `admin`: 결제기록, 대기열 관리, 세션 시작/종료, 연장, 노쇼 처리, 통계 확인
- `board`: 대기번호 표시, 현재 이용 현황, TTS 안내

이 프로젝트는 화려한 일반 웹서비스보다 `현장 안정성`, `낮은 운영비`, `단순한 관리`, `저사양 서버 대응`을 우선한다.

## 2. 최종 기술스택

### 애플리케이션

- 프론트엔드: `Next.js App Router`
- 백엔드: `Next.js Route Handlers` + 필요 시 `Server Actions`
- 언어: `TypeScript`
- UI 스타일링: `Tailwind CSS`
- 입력 검증: `Zod`
- 날짜 처리: `date-fns`
- TTS: 브라우저 `Web Speech API (speechSynthesis)`

### 데이터베이스

- DBMS: `PostgreSQL`
- 개발 초기에는 메모리 기반 데모 저장소를 둘 수 있으나, 운영 기준 저장소는 PostgreSQL로 통일한다.
- ORM은 추후 도입 시 `Drizzle ORM`을 우선 검토한다.

### 운영 환경

- 서버 OS: `Ubuntu Server LTS`
- 앱 런타임: `Node.js LTS`
- 프로세스 관리: `systemd`
- 리버스 프록시: `Caddy`
- 백업: `pg_dump + cron`

### 배포 구조

- 서버 전용 PC 1대에 앱과 DB를 함께 설치한다.
- 모니터 없는 헤드리스 서버를 기본 전제로 한다.
- 관리자PC, 키오스크 태블릿, 보드용 화면은 브라우저로 내부망 주소에 접속한다.

예시:

- `http://192.168.x.x/admin`
- `http://192.168.x.x/kiosk`
- `http://192.168.x.x/board`

## 3. 아키텍처 원칙

### 단일 코드베이스 원칙

- 프론트엔드와 백엔드는 하나의 Next.js 프로젝트에서 관리한다.
- Spring, NestJS, 별도 Java 백엔드는 도입하지 않는다.
- 운영 복잡도를 늘리는 불필요한 마이크로서비스 분리는 하지 않는다.

### 저사양 서버 우선 원칙

- 서버 사양이 낮기 때문에 메모리와 CPU 사용량을 항상 보수적으로 설계한다.
- 대형 라이브러리, 무거운 실시간 클라이언트, 과한 애니메이션, 불필요한 SSR 부담을 피한다.
- 한 화면에서 과도한 폴링이나 과도한 클라이언트 상태 저장을 만들지 않는다.

### 운영 단순성 우선 원칙

- 센터 담당자가 이해하고 유지 가능한 구조를 택한다.
- 장애가 났을 때 원인을 빨리 찾을 수 있도록 단순한 로그 구조를 유지한다.
- 기능 추가보다 `현장 동작 안정성`을 우선한다.

## 4. 도메인 원칙

핵심 엔터티는 아래 기준으로 유지한다.

- `members`: 학생/보호자 기본 정보
- `resources`: 개별 장비 단위 자원
- `pricing_rules`: 시간권 및 연장권
- `visits`: 방문 단위
- `queue_entries`: 대기 단위
- `sessions`: 실제 사용 단위
- `payments`: 결제기록
- `tts_events`: 음성 안내 이벤트
- `staff_activity_logs`: 직원 조작 로그

### 자원 기준

- 컴퓨터: 6대
- 닌텐도: 4대
- 플스: 2대

### 운영 규칙

- 회원은 최초 1회 등록 후 재방문 조회 방식으로 운영한다.
- 결제는 PG 연동 없이 `기록만` 남긴다.
- 사용 시작 시점은 관리자가 확정한다.
- 대기열은 자원 유형별 FIFO가 기본이다.
- 공개 보드는 번호 중심 표시를 기본으로 하고, 이름 호출은 설정으로 전환 가능하게 한다.
- 종료 임박 알림과 종료 알림은 TTS 이벤트로 생성한다.

## 5. 라우팅 규칙

사용자 화면 라우트는 아래를 기본으로 한다.

- `/`: 홈 대시보드
- `/kiosk`: 키오스크 화면
- `/admin`: 관리자 화면
- `/board`: 대기 보드 화면

API 라우트는 `/api/*` 아래에 둔다.

예시:

- `/api/state`
- `/api/members`
- `/api/visits`
- `/api/queue`
- `/api/sessions`
- `/api/payments`
- `/api/reports`
- `/api/tts-events`

새 API는 `리소스 기준`으로 추가하고, 화면별 API 네이밍은 피한다.

## 6. 데이터 접근 규칙

- DB 접근 로직은 `src/lib/server` 또는 `src/server` 계층에 둔다.
- 화면 컴포넌트에서 SQL이나 DB 드라이버를 직접 호출하지 않는다.
- 비즈니스 로직은 라우트 파일 안에 길게 넣지 말고 서비스 함수로 분리한다.
- 입력 검증은 모든 쓰기 요청에서 `Zod`를 통해 수행한다.
- 금액, 시간, 상태값은 문자열 하드코딩 대신 enum 또는 상수 테이블로 관리한다.

## 7. UI/프론트엔드 규칙

- 키오스크는 큰 버튼, 짧은 흐름, 최소 입력을 우선한다.
- 관리자 화면은 마케팅형 화면이 아니라 운영형 화면으로 만든다.
- 대기 보드는 멀리서 봐도 읽히는 큰 숫자와 높은 대비를 사용한다.
- 다크톤 기반 UI는 유지하되, 정보 우선 구조를 해치지 않는 범위에서만 시각 효과를 사용한다.

### React/Next 규칙

- 불필요한 전역 상태 라이브러리는 기본 도입하지 않는다.
- 가능한 한 서버 컴포넌트를 기본으로 하고, 상호작용이 필요한 부분만 클라이언트 컴포넌트로 둔다.
- 폴링, TTS, 브라우저 API 사용은 클라이언트 컴포넌트에서만 처리한다.
- 클라이언트 상태는 꼭 필요한 최소 범위만 유지한다.

## 8. 코딩 규칙

- 언어는 기본적으로 `TypeScript strict` 기준을 따른다.
- 파일명은 소문자 kebab-case 또는 현재 프로젝트 규칙을 따른다.
- 타입, 상수, 유틸은 재사용 가능하도록 `src/lib` 아래에 정리한다.
- 컴포넌트는 과도하게 비대해지기 전에 분리한다.
- 주석은 필요한 경우만 짧고 명확하게 쓴다.
- 임시 콘솔 로그는 커밋 전에 제거한다.

### 금지 항목

- `any` 남발
- 화면 컴포넌트 내부에 긴 비즈니스 로직 작성
- API route 안에서 중복 검증 코드 반복
- 운영 상태를 localStorage 단독 저장소로 사용하는 방식
- 서버 전용 정보가 클라이언트 번들로 새어 나가는 구조

## 9. 운영/배포 규칙

- 운영 모드는 반드시 `production build`로 실행한다.
- `next dev`를 운영 서버에서 사용하지 않는다.
- 앱은 `systemd` 서비스로 등록한다.
- PostgreSQL도 시스템 서비스로 등록한다.
- 서버 재부팅 후 자동으로 앱과 DB가 같이 올라와야 한다.
- 방화벽은 내부망 접근만 허용하는 방향을 우선한다.

### 백업 규칙

- 최소 하루 1회 DB 백업
- 최소 주 1회 외부 저장소 복사
- 복구 테스트를 실제로 해본 백업만 신뢰한다

## 10. 성능 규칙

- 관리자/보드 동기화는 가능한 한 가볍게 유지한다.
- 실시간성이 꼭 필요해도 초기에는 단순한 구조를 우선한다.
- 이미지, 동영상, 무거운 차트 라이브러리는 최소화한다.
- 메모리 4GB 서버를 기준으로 기능을 설계한다.

## 11. 보안/개인정보 규칙

- 학생 이름, 학년/나이, 보호자 연락처는 최소 범위만 저장한다.
- 공개 보드에는 보호자 연락처와 민감정보를 절대 노출하지 않는다.
- 환경변수와 DB 접속정보는 저장소에 커밋하지 않는다.
- 관리자 기능은 추후 최소한의 인증 또는 내부망 제한을 전제로 설계한다.

## 12. 문서화 규칙

- 중요한 운영 규칙 변경 시 `Readme.md`와 본 문서를 함께 갱신한다.
- DB 스키마 변경 시 마이그레이션 파일을 반드시 남긴다.
- 화면 흐름이 바뀌면 라우트와 주요 사용자 시나리오를 문서에 반영한다.

## 13. 우선순위 기준

기능 판단이 애매할 때는 아래 순서로 우선순위를 둔다.

1. 현장에서 멈추지 않는가
2. 운영자가 쉽게 이해할 수 있는가
3. 저사양 서버에서 안정적으로 도는가
4. 유지보수가 단순한가
5. 그다음에야 코드 미관과 확장성을 본다

## 14. 현재 프로젝트의 기본 결론

- 백엔드는 `Next.js`로 간다.
- 서버는 `Ubuntu Server` 자체호스팅으로 간다.
- DB는 `PostgreSQL`로 간다.
- 운영은 `내부망 브라우저 접속`을 기본으로 한다.
- 외부 클라우드 의존은 최소화한다.

## 15. Ubuntu 자체호스팅 운영 아티팩트

아래 파일들은 실제 서버 설치와 운영에 바로 쓸 수 있는 기준 아티팩트다.

- `deploy/systemd/autopan.service`
- `deploy/systemd/autopan-backup.service`
- `deploy/systemd/autopan-backup.timer`
- `deploy/caddy/Caddyfile`
- `scripts/backup-postgres.sh`
- `docs/ubuntu-self-hosting.md`
- `docs/local-network-runbook.md`

운영 원칙은 다음과 같다.

- 앱은 `127.0.0.1:3000`에서만 띄우고 Caddy가 외부 요청을 받는다.
- 백업은 `pg_dump` 기반으로 매일 한 번 실행한다.
- 서비스 재시작과 서버 재부팅 후 자동 기동을 최우선으로 둔다.
- 내부망 장비는 서버의 고정 IP 또는 예약된 LAN 주소로 접속한다.
